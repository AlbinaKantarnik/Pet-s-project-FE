import { Icon } from "./Icon"
import React from 'react';

export const SearchIcon = ({ className }) => {

    return (
        <span title="Save for later" >
            <Icon i="search" className={className}/>
        </span>
    )
}
